# Obsidian requires the release tag to exactly match manifest.json's version
# with no leading "v". npm prepends "v" to version tags by default, so disable
# that: `npm version 0.1.1` then creates the tag `0.1.1`, not `v0.1.1`.
tag-version-prefix=""
